{
  "name": "tiendanube-bundle-app",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/server",
    "packages/admin",
    "packages/storefront"
  ],
  "dependencies": {
    "serverless-http": "^3.2.0"
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace packages/server",
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces"
  }
}
